— помощью объектов и методов std::string, std::vector написать программу,
котора€ принимала бы на вход строку (последовательность символов, 
не содержащую символов перевода строки) и выводила бы в качестве результата текст, 
состо€щий из тех же самых слов, что и исходна€ строка, но которые следуют в пор€дке возрастани€ длины слова. 
«а слово считаетс€ люба€ последовательность не-пробелов. —лова в исходном тексте могут быть разделены несколькими пробелами, но в выходном тексте достаточно одного. 

ѕример:  УC++ is a very      performant language.\nФ  -> 
                  Уa is C++ very language. performantФ
